using VContainer;

namespace JaehyeokSong0.Tacidto.Application.Scope
{
    public class LoadingSceneScope : SceneScopeBase
    {
        protected override void Configure(IContainerBuilder builder)
        {
            base.Configure(builder);
        }
    }
}